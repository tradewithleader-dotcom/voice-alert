# Voice Alert App
This app sends alerts to Telegram via webhook.

## How to Run Locally
1. Install Python 3.x
2. Run: pip install -r requirements.txt
3. Create config.json with bot token & chat ID
4. Run: python app.py
